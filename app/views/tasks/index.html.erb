<h2>Task List</h2>
<p><%= notice %></p>
<table>
  <thead>
    <tr>
      <th>Task name</th>
      <th>Details</th>
      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.task_name %></td>
        <td><%= task.content %></td>
        <td><%= link_to 'Show', task_path(task.id) %></td>
        <td><%= link_to "Edit", edit_task_path(task.id), data: { confirm: '本当に編集していいですか？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
