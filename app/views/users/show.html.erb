<br>
<div class="container" id="main_layout">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h3>Account information</h3>
      <p>Name:</p>
      <p><%= @user.name %></p>
      <p>Email:</p>
      <p><%= @user.email %></p>
      <p>Task:</p>
      <p><%= @user.tasks.size %></p>
      <p>Admin:</p>
      <p><%= @user.admin %></p>
      <br>
      <%= link_to "back to the list", admin_users_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>
